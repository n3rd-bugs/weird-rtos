# Setup configuration options.
if (${CONFIG_BOOTLOAD})
    setup_option_def(BOOTLOADER_LOADED ON DEFINE "If bootloader is already loaded.")
    setup_option_def(BOOTLOAD_MMC ON DEFINE "If we want to bootload from MMC card.")
    setup_option_def(BOOTLOAD_STK ON DEFINE "If we want to bootload from STK.")
    if (${BOOTLOAD_MMC})
        setup_option_def(BOOTLOAD_MMC_HEX_NONLINEAR OFF DEFINE "If HEX on MMC can be nonlinear.")
        setup_option_def(BOOTLOAD_MMC_CS_PIN_NUM 3 INT "MMC SPI slave-select (CS) pin number to be used by MMC bootloader." VALUE_LIST atmega_pinnum_values)
        setup_option_def(BOOTLOAD_MMC_CS_DDR DDRA MACRO "MMC SPI slave-select (CS) DDR to be used by MMC bootloader." VALUE_LIST atmega_ddr_values)
        setup_option_def(BOOTLOAD_MMC_CS_PORT PORTA MACRO "MMC SPI slave-select (CS) PORT to be used by MMC bootloader." VALUE_LIST atmega_port_values)
        setup_option_def(BOOTLOAD_MMC_CLK_PIN_NUM 2 INT "MMC SPI clock (CLK) pin number to be used by MMC bootloader." VALUE_LIST atmega_pinnum_values)
        setup_option_def(BOOTLOAD_MMC_CLK_DDR DDRA MACRO "MMC SPI clock (CLK) DDR to be used by MMC bootloader." VALUE_LIST atmega_ddr_values)
        setup_option_def(BOOTLOAD_MMC_CLK_PORT PORTA MACRO "MMC SPI clock (CLK) PORT to be used by MMC bootloader." VALUE_LIST atmega_port_values)
        setup_option_def(BOOTLOAD_MMC_DI_PIN_NUM 5 INT "MMC SPI data-in (DI) pin number to be used by MMC bootloader." VALUE_LIST atmega_pinnum_values)
        setup_option_def(BOOTLOAD_MMC_DI_DDR DDRA MACRO "MMC SPI data-in (DI) DDR to be used by MMC bootloader." VALUE_LIST atmega_ddr_values)
        setup_option_def(BOOTLOAD_MMC_DI_PORT PORTA MACRO "MMC SPI data-in (DI) PORT to be used by MMC bootloader." VALUE_LIST atmega_port_values)
        setup_option_def(BOOTLOAD_MMC_DO_PIN_NUM 0 INT "MMC SPI data-out (DO) pin number to be used by MMC bootloader." VALUE_LIST atmega_pinnum_values)
        setup_option_def(BOOTLOAD_MMC_DO_DDR DDRA MACRO "MMC SPI data-out (DO) DDR to be used by MMC bootloader." VALUE_LIST atmega_ddr_values)
        setup_option_def(BOOTLOAD_MMC_DO_PIN PINA MACRO "MMC SPI data-out (DO) PIN to be used by MMC bootloader." VALUE_LIST atmega_pin_values)
        
    endif ()
    
    # Setup AVR dude options.
    if (PLATFORM STREQUAL atmega1284)
        setup_option_def(${PLATFORM}_DUDE_MCU "m1284p" STRING "AVR dude MCU.")
        setup_option_def(${PLATFORM}_DUDE_DRIVER "arduino" STRING "AVR dude driver.")
        setup_option_def(${PLATFORM}_DUDE_SER "COM6" STRING "AVR dude serial port.")
        setup_option_def(${PLATFORM}_DUDE_BOUD "115200" STRING "AVR dude boudrate.")
     endif ()
endif ()